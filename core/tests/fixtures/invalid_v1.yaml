# 负例 V1：属地 CN 却配 America/New_York 时区（SPEC §1 fixtures）
# 仅触发 LINT_V1；其余恒真规则（V2/V3/V4/V5/V7/V8/V9/V10）刻意保持合法，
# 以保证本用例只验证属地族一致性这一条规则（SPEC §9.5）。
version: 1
meta:
  id: p_20260727_b1v1
  name: 时区矛盾替身
  created_at: "2026-07-27T22:40:00+08:00"

region:
  follow_ip: true
  detected_ip_region: CN
  locale: zh-CN
  timezone: America/New_York    # ← V1：与 CN 属地族矛盾
  languages: [zh-CN, zh, en-US, en]

os:
  family: linux
  distro: ubuntu-24.04
  chrome_channel: stable

hardware:
  gpu:
    mode: host_passthrough
    renderer_string_source: real
  hardwareConcurrency: 8
  deviceMemory: 8

display:
  width: 1920
  height: 1080
  dpr: 1.0

fonts:
  pack: linux-noto-standard
  extras: [cjk]

webrtc:
  ip_handling_policy: disable_non_proxied_udp

farbling:
  seed: 9f2c4e8a1b3d5f7046a8c2e1d3b5f709
  scope: [canvas_readback, webgl_readback, audio]

evolution:
  strategy: anchor_chrome_version
  baseline_chrome: 138.0.7204.0
  drift_policy: follow_stable_diff

proxy: null

storage:
  profile_volume: ts_p_20260727_b1v1_profile
  log_volume: ts_p_20260727_b1v1_logs
  retain_days: 30

monitoring:
  keylog: true
  log_encryption: age

lifecycle:
  state: active
