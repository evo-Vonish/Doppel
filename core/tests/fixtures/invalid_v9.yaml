# 负例 V9：WebRTC 策略为 default（SPEC §1 fixtures）
# 仅触发 LINT_V9；其余恒真规则刻意保持合法（SPEC §9.5）。
version: 1
meta:
  id: p_20260727_b9v9
  name: WebRTC 泄露替身
  created_at: "2026-07-27T22:40:00+08:00"

region:
  follow_ip: true
  detected_ip_region: CN
  locale: zh-CN
  timezone: Asia/Shanghai
  languages: [zh-CN, zh, en-US, en]

os:
  family: linux
  distro: ubuntu-24.04
  chrome_channel: stable

hardware:
  gpu:
    mode: host_passthrough
    renderer_string_source: real
  hardwareConcurrency: 8
  deviceMemory: 8

display:
  width: 1920
  height: 1080
  dpr: 1.0

fonts:
  pack: linux-noto-standard
  extras: [cjk]

webrtc:
  ip_handling_policy: default  # ← V9：default 档会经 WebRTC 泄露本机真实 IP

farbling:
  seed: 9f2c4e8a1b3d5f7046a8c2e1d3b5f709
  scope: [canvas_readback, webgl_readback, audio]

evolution:
  strategy: anchor_chrome_version
  baseline_chrome: 138.0.7204.0
  drift_policy: follow_stable_diff

proxy: null

storage:
  profile_volume: ts_p_20260727_b9v9_profile
  log_volume: ts_p_20260727_b9v9_logs
  retain_days: 30

monitoring:
  keylog: true
  log_encryption: age

lifecycle:
  state: active
